// Define the string
let str = "harshini umesh";

// Initialize a counter to track the length
let count = 0;

// Loop through the string and count each character
for (let i = 0; str[i] !== undefined; i++) {
  count++;
}

// Print the length
console.log("The length of the string is:", count);